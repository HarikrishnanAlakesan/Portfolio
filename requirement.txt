# Core framework
streamlit==1.35.0

# For rendering and web interaction
pillow>=10.0.0  # Required for handling images in some cases

# Optional: If you plan to use any image, video, or audio handling
opencv-python-headless>=4.10.0.0

# Optional: If deploying on cloud and need secure requests
requests>=2.31.0

# Optional: For markdown rendering improvements (if needed)
markdown

# Optional: If you use any advanced HTML parsing or scraping (not required for this app)
# beautifulsoup4
